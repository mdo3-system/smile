# 固定業務ロジック（FIXED_LOGIC）
## バージョン 1.0.28 時点の確定仕様

---

## 1. 画面構成と各DBの表示内容

### 管理者画面（project_detail.php - 4カラム）

| カラム | 内容 |
|--------|------|
| **カラム1** 案件情報・依頼主図書 | 基本情報、見積書PDF、見積時の受領図面、必要図書ステータス、依頼主アップロード図書（インライン） |
| **カラム2** スケジュール | `col_schedule.php`: 進捗スケジュール表（営業日自動計算・実施日入力フォーム付き）|
| **カラム3** 成果物・見積 | `col_center_deliverables.php`: 管理者成果物UP、`col_estimator.php`: 自動見積シミュレーター |
| **カラム4** チャット・管理ツール | `col_right.php`: チャット（LINEスタイル）、ステータス変更、入金管理 |

### 依頼主画面（project_detail.php - 3カラム）

| カラム | 内容 |
|--------|------|
| **カラム1** 案件情報・見積状況 | 基本情報、全体スケジュール表、請求・支払状況、見積書PDF、見積時の受領図面、設計依頼ボタン |
| **カラム2** 成果物・アップロード | `col_center_deliverables.php`: 管理者からの成果物（依頼主には閲覧のみ）、`col_center_uploads.php`: 依頼主からの図書アップロード（インライン履歴付き） |
| **カラム3** チャット | `col_right.php`: チャット（LINEスタイル） |

---

## 1.2 業務ロールと関係性ルール（完全分離）

本システムは以下の4つの業務ロールが存在し、明確に役割が分離されている。

1. **あなた（設計管理者 / admin）**:
   - 設計業務の全般を統括する。
   - 依頼主から図書や構造仕様を受け取り、設計・スケジュールを決定する。
   - 協力業者（お手伝いさん）に対して意匠図・構造図の作図発注を行い、納品されたファイルを検収する。
2. **依頼主（client）**:
   - 案件の作成、見積もり確認、正式な設計依頼、必要図書のアップロードを行う。
   - チャットを通じてあなた（設計管理者）と仕様調整を行う。
   - 入金や請求書の確認などの金銭面でのみ、経理ユーザーと関わりを持つ。
3. **経理（accountant）**:
   - 案件の入出金（50%着手金・残金）の確認、および請求・支払状況の更新のみを行う。
   - 設計成果物やCADデータなどの技術的な資料、協力業者への作図指示には一切関与しない。
4. **協力業者（subcontractor）**:
   - あなた（設計管理者）から発注を受け、作図（意匠図・構造図）を行う。
   - 納品データはすべてあなた（設計管理者）宛に提出する。
   - 支払い（報酬）の請求管理でのみ、経理ユーザーと関わりを持つ。

### 相互関係とアクセス制御の鉄則
- **非接触ルール**: 「依頼主」と「協力業者」はシステム上で直接のやり取りをすることは一切できず、チャットスレッドやアップロードファイルも互いに完全に不可視である。
- **納品物の検証フロー**: 協力業者が作成・納品した構造図（`sub_structural_pdf` 等）は、あなた（設計管理者）が内容を確認して「承認」ボタンを押すまでは、依頼主画面には絶対に公開されない（自動公開は厳禁）。必ずあなたによる検収・承認フローを経る。

---

## 1.5 チャットスレッドの完全分離ルール（絶対厳守）
チャットは情報の混入を防ぐため、以下のスレッドタイプ (`thread_type`) で物理的に完全分離する。
- **`client_admin`**: 「依頼主」と「管理者」の間のチャット。
- **`sub_admin`**: 「協力業者」と「管理者」の間のチャット。
※依頼主と協力業者が直接チャットすることはなく、システム上でも両者のメッセージが混入してはならない。API (`api_send_message.php`) でのハードコードや誤った上書きは厳禁とする。

---

## 2. 見積時の受領図面（見積時PDF）と設計後図書の分離

### 見積時受領図面（`pdf_*` カテゴリ）
- `new_request.php` でのPDF提出時に登録される
- カテゴリ: `pdf_plan`, `pdf_elevation`, `pdf_layout`, `pdf_section`, `pdf_area_calc`
- **★見積依頼時は意匠図PDFのみ**: CADデータ・確認申請書・地盤調査報告書・外皮系資料は展示・受付不可。概算見積のための最低限資料のみ受付ける。
- **表示場所**: 左パネル「📋 見積時の受領図面」ボックス（管理者・依頼主の両方に表示）
- **編集不可**: 見積書と紐づいているため、後から変更しない

### 設計依頼後アップロード図書（`cad_*`, `app_doc`, `soil_*`, `spec_*` 等）
- 設計依頼（`request_design_start` action）時以降にアップロードされる
- **表示場所**: 中央パネル「依頼主アップロード図書」ボックス（`col_center_uploads.php`）
- **履歴管理**: バージョン管理あり（最新版を太字表示、過去版はプルダウン）
- **差し替え**: ファイル存在時は「差し替え理由（必須）」入力後にUP

---

## 3. 依頼種別と必要図書一覧

案件作成時に選択された依頼種別に応じて、以下の図書が必要となる。

> **【確定ルール】見積依頼フェーズ（quote_req）の資料**  
> 見積依頼時にアップロードできるのは **意匠図のPDFのみ**（平面図・立面図・配置図・矩計図・求積図）。  
> 確認申請書・地盤調査報告書・CADデータ・外皮系資料は **見積依頼時には不要**。  
> 正式な設計依頼（`request_design_start`）以降に必要図書をアップロードする。

### ① 許容応力度設計（req_permit=1）
**必要図書:**
- 意匠CADデータ（配置図, 1F/2F平面図, 立面図, 矩計図）
- 確認申請書（2面〜5面）← 後出し可、提出日が一次回答起算日
- 地盤調査報告書 ← 後出し可、提出日が一次回答起算日

**構造材種の指定:** 必要（許容応力度・基礎梁計算でのみ入力させる）

**スケジュール目安:** 一次回答まで **12営業日**

### ② 性能表示壁量計算（req_wall=1）
**必要図書:**
- 意匠CADデータ（配置図, 1F平面図, 立面図）
- 確認申請書（2面〜5面）← 後出し可
- 地盤調査報告書 ← **不要**（壁量計算では地盤は関係なし）

**構造材種の指定:** **不要**（壁量計算では材種指定は行わない）

**スケジュール目安:** 一次回答まで **7営業日**

### ③ 外皮計算（req_skin=1）
**必要図書:**
- 意匠CADデータ（配置図, 1F/2F平面図, 立面図, 矩計図）
- 仕様書（断熱材）
- サッシ・玄関ドア仕様書
- 24時間換気計算図書
- 設備機器カタログ

**構造材種の指定:** **不要**

**スケジュール目安:** 一次回答まで **10営業日**

### ④ 天空率計算（req_sky=1）
**必要図書（見積内容に応じて判定・自動表示）:**
- 道路の資料（座標・測量図・道路台帳等）← `天空率 道路斜線` が見積に含まれる場合
- 真北の資料 ← `天空率 北側斜線` が見積に含まれる場合
- ※見積の `note` JSONに含まれる `name` フィールドを検査して自動判定する

**構造材種の指定:** **不要**

**スケジュール目安:** 一次回答まで **10営業日**

### ⑤ 基礎・横架材許容応力度（req_opt_kisohari=1）
- 許容応力度設計（①）の図書に準じる
- 地盤調査報告書 ← **必要**
- **構造材種の指定:** 必要
- **スケジュール目安:** 一次回答まで **12営業日**

### 補足図書（追加・差し替え時）
- 補正通知書（`correction_notice`）
- その他追加資料（`other_extra`）
- 地盤改良関連図書（`soil_impr`）← 地盤調査結果が「改良あり」の場合

---

## 4. 一次回答の要件・フロー

```
[見積依頼受付] → [管理者が見積シミュレーターで金額算出・PDF発行]
   ↓
[依頼主が見積確認・設計依頼ボタンから]
   ① 構造仕様を入力（木材の種類・壁の仕様等）
   ② 必要図書を個別スロットにアップロード
   ③ 「見積時から図面変更の有無」を選択・入力（必須）
   ↓ action: request_design_start
[ステータス: primary_prep に更新]
[チャットに「設計開始依頼」通知 + 管理者へメール]
   ↓
[管理者が図書を確認]
   ↓
[管理者が一次回答期日を設定]
   → action: set_primary_due_date
   → projects.primary_due_date を更新
   → ステータス: contracted に更新（primary_prep → contracted）
   → チャットに「スケジュール確定」通知
   ↓
[スケジュール表が全カラムで更新される]
```

---

## 5. スケジュール計算ロジック（営業日・玉突き方式）

`functions.php` の `addBusinessDays($dateStr, $days)` を使用。
**水曜・日曜をスキップ**して加算（土曜は稼働日扱い）。

### 許容応力度計算ステップ (中間金マイルストーン追加後)
| ステップ番号 | ステップ | 担当 | 計算基準 | 加算 |
|---|---------|------|---------|------|
| 0 | 設計図書の受領 | 依頼主 | 開始時 | - |
| 1 | 着手基準日（一次回答） | サポート | = `primary_due_date`（管理者設定） | - |
| 2 | 構造計算・図面 初回提示 | サポート | 一次回答から | +10営業日 |
| 3 | 一次回答CB（内容確認） | 依頼主 | 初回提示から | +4営業日 |
| 4 | 中間金（50％）のご入金 | 依頼主 | 一次回答CB確定後 | +0営業日 (即時) |
| 5 | 構造図UP | サポート | 中間金確認後から | +4〜7営業日 |
| 6 | 構造図CB | 依頼主 | 構造図UPから | +4〜7営業日 |
| 7 | 修正図面UP | サポート | CB確認から | +3営業日 |
| 8 | 申請図書一式UP | サポート | 修正UPから | +3営業日 |
| 9 | 質疑・審査待機 | 確認機関 | 申請図書UPから | +30日（暦日） |
| 10 | 補正対応 | サポート | 質疑受領から | +7営業日 |
| 11 | 残金のご精算 | 依頼主 | 完了後 | +7日（暦日） |

※ 壁量・外皮・天空率等の各スケジュールにも同様に「申請図書一式UP」の直前に「中間金（50％）のご入金」が挿入され、インデックスが+1シフトします。

### 実施日（実績）の玉突き
- 管理者は各ステップの「実施日」を入力できる（`update_schedule_actual` action）
- 実施日が入力されると、その日が次工程の**起算日**となる
- `projects.schedule_actuals` カラム（JSON型）に `{ ステップ番号: "YYYY-MM-DD" }` 形式で保存

---

## 6. 初期見積額の自動設定

- **タイミング**: 管理者が見積PDFを初回発行したとき（`api_save_estimate.php`）
- **条件**: `projects.initial_est_amount` が `0` または `NULL` の場合のみ上書き
- **保存先**: `projects.initial_est_amount`（税抜）、`projects.initial_est_date`（発行日）
- **依頼主画面での表示**: 「ご請求・お支払い状況」ボックスに表示
- **再発行時**: 2回目以降の見積発行では `initial_est_amount` は変更しない（`formal_est_amount` を使用）

---

## 7. ステータス遷移表

| ステータスキー | 日本語 | 遷移トリガー |
|--------------|--------|------------|
| `quote_req` | 見積依頼 | 新規案件作成時 |
| `doc_submitted` | 図書提出済 | 依頼主の「設計依頼」ボタン実行時 |
| `primary_prep` | 一次回答準備中 | 同上 |
| `contracted` | スケジュール確定 | 管理者が一次回答期日を設定したとき |
| `structural_dwg` | 構造図作成中 | 管理者が協力業者へ構造図発注したとき |
| `submission` | 提出済・確認中 | 管理者が納品承認したとき |
| `correction` | 補正対応中 | 手動変更 |
| `completed` | 完了 | 手動変更 |

---

## 8. ファイル管理の仕様

### カテゴリと用途（`project_files.file_category`）

| カテゴリキー | 用途 | 管理者/依頼主 |
|------------|------|-------------|
| `pdf_plan` | 平面図（見積時受領） | 依頼主 |
| `pdf_elevation` | 立面図（見積時受領） | 依頼主 |
| `pdf_layout` | 配置図（見積時受領） | 依頼主 |
| `pdf_section` | 矩計図（見積時受領） | 依頼主 |
| `pdf_area_calc` | 求積図（見積時受領） | 依頼主 |
| `cad_layout` ～ `cad_section` | 設計依頼後のCADデータ | 依頼主 |
| `app_doc` | 確認申請書 | 依頼主 |
| `soil_report` | 地盤調査報告書 | 依頼主 |
| `correction_notice` | 補正通知書（複数可） | 依頼主 |
| `structural_dwg` | 構造図（成果物） | 管理者 |
| `calc_doc` | 構造計算書 | 管理者 |
| `wall_calc_doc` | 壁量計算書 | 管理者 |
| `skin_calc_doc` | 外皮計算書 | 管理者 |
| `sky_calc_doc` | 天空率計算書 | 管理者 |
| `inv_primary` | 請求書（一次） | 管理者 |

### バージョン管理ルール
- `is_latest = 1`: 最新版（1つだけ）
- `is_latest = 0`: 旧バージョン（履歴として保持）
- 差し替え時は旧版を `is_latest = 0` に更新後、新版を `version + 1` でINSERT
- 差し替え理由は `update_reason` カラムに保存し、チャットへ自動投稿

### Google Drive 保存階層ルール
- **自動分類フォルダ構成**: アップロードされるすべてのファイル（意匠図PDF、設計資料CAD、チャット添付ファイルなど案件に紐づくすべてのファイル）は、案件ごとに自動で以下のフォルダ構成に分類して保存される。
  `設計サポート / 【クライアント名（企業名または担当者名）】 / 【案件名】`
- **対象となるファイル**:
  - 新規見積依頼時の意匠図PDF
  - 設計依頼後のCADデータ、確認申請書、地盤調査報告書など
  - チャット（依頼主・管理者間、および協力業者・管理者間）の添付ファイル
  - 管理者がアップロードする成果物（構造図、計算書等）および請求書PDF
- **フォルダ生成キャッシュ**: クライアントフォルダIDは `users.drive_folder_id` に、案件フォルダIDは `projects.drive_folder_id` に保存され、2回目以降のアップロード時はキャッシュされたIDが利用される。


---

## 9. 構造材種・屋根部材の入力仕様

### 構造材種が必要な依頼種別
- **必要**: 許容応力度設計（req_permit=1）、基礎横架材計算（req_opt_kisohari=1）
- **不要**: 壁量計算（req_wall=1）、外皮計算（req_skin=1）、天空率（req_sky=1）

### 入力フィールドの構成（upload_slots.php 内の「構造材種」セクション）
| フィールド名 | 入力形式 | 例 |
|------------|--------|----|
| `spec_dodai` | 材種 + 断面 | ﾋﾉｷKD □105、□120（プルダウン：ﾋﾉｷKD、ﾍﾞｲﾏﾂ、ﾍﾞｲﾂｶﾞKD、その他 ＋ 断面等テキスト入力） |
| `spec_obiki` | 材種 + 断面 | ﾋﾉｷKD □90、□105（プルダウン：ﾋﾉｷKD、ﾍﾞｲﾂｶﾞKD、ｽｷﾞKD、その他 ＋ 断面等テキスト入力） |
| `spec_hashira` | 材種 + 断面 | ｽｷﾞKD □105、□120（プルダウン：ﾋﾉｷKD、ｽｷﾞKD、ｽｷﾞ集成、WW集成、その他 ＋ 断面等テキスト入力） |
| `spec_hari` | 材種・断面 | ﾍﾞｲﾏﾂKD □105、□120（プルダウン：ﾍﾞｲﾏﾂKD、ｽｷﾞKD、ｽｷﾞ集成、RE集成、ﾊｲﾌﾞﾘｯﾄﾞ集成、その他 ＋ 断面等テキスト入力） |
| `spec_koyatsuka` | 材種 + 断面 | ｽｷﾞKD □90、□105（プルダウン：ｽｷﾞKD、ﾍﾞｲﾏﾂKD、その他 ＋ 断面等テキスト入力） |
| `spec_moya` | 材種 + 断面 | ｽｷﾞKD □90、□105（プルダウン：ｽｷﾞKD、ﾍﾞｲﾏﾂKD、その他 ＋ 断面等テキスト入力） |
| `spec_munagi` | 材種 + 断面 | ｽｷﾞKD □90、□105（プルダウン：ｽｷﾞKD、ﾍﾞｲﾏﾂKD、その他 ＋ 断面等テキスト入力） |
| `spec_taruki` | **W×H@間隔mm** | 45×60@364（プルダウン：ﾍﾞｲﾏﾂKD、ｽｷﾞKD、その他 ＋ 各数値・ピッチ等テキスト入力） |
| `spec_kanamono` | フリーテキスト | Z金物、Tec-One等 |
| `spec_wall` | フリーテキスト | 構造用合板 9mm+EXハイパー |

---


## 10. メール通知の仕様

### 送信元
- `system@thanks.work`

### 通知タイミングと対象
| タイミング | 送信先 | 処理場所 |
|-----------|--------|--------|
| 見積依頼（新規案件作成） | 管理者（info@thanks.work）| `new_request.php` |
| 見積依頼（初回確認） | 依頼主（users.email） | `new_request.php` |
| 管理者がチャット送信 | 依頼主（users.email） | `actions/action_chat.php` |
| 管理者が成果物をUP | 依頼主（users.email） | `actions/action_upload_file.php` |
| 正式依頼受付 | 管理者（info@thanks.work） | `actions/action_save_specs.php` |

### メールアドレスの保存先
- `users.email` カラム（varchar(255), Nullable）
- 初回登録タイミング: `new_request.php` の `contact_email` フィールド入力時
- 既存ユーザーへの既存チャット通知との後方互換: チャット本文の正規表現解析は廃止し、`users.email` から直接取得する

---

## 11. 本見積確定と請求・入金管理ロジック (v1.1.15〜1.1.17)

### ① 本見積確定機能
- **確定処理**: 管理者用見積シミュレーター (`col_estimator.php`) から「本見積として確定・PDF発行」を行うと、`projects.formal_est_amount`（本見積額）および `projects.formal_est_date` が設定される。
- **金額判定**: 初回概算見積（初期見積）と同額であっても、明示的に本見積として確定できる。

### ② 一次請求書 (着手金50%分) PDF発行
- **発行条件**: 本見積額（`formal_est_amount`）が登録されている場合、管理者画面から発行可能。
- **金額算出式**: `本見積額（税別） × 0.5` に消費税 10% を加算した金額。
- **PDF保存・登録**: Google DriveにPDFがアップロードされ、`project_files` にカテゴリ `inv_primary` として登録される。
- **自動チャット通知**: クライアントチャット (`client_admin`スレッド) へ自動で「【一次請求書(50%)が発行されました】」のメッセージとPDFダウンロードリンクが送信される。

### ③ 入金済額連動の残金精算額（再集金）減額表示
- **計算式**: `最終ご請求額（残金精算額 / 再集金） = (本見積額 ＋ 追加費用) - 入金済額`
- **表示場所**: 管理者画面 (`col_left.php`) およびクライアント画面 (`dashboard_client.php`) の金銭・支払状況欄。
- **仕様**: 経理が「入金済額」を入力・保存すると、即座に「最終ご請求額」が減額反映される。

---

## 12. 協力業者発注フローとCAD公開制御 (v1.1.18)

### ① CADデータ一時公開制御
- **仕様**: 管理者が協力業者へ作図発注 (`subcontractor_orders`) をした際、発注ステータスが「承諾待ち (`requested`)」から「承諾または辞退決定（`accepted` / `rejected`）」するまでの間、および作業中（`accepted`）の間、一時的に依頼主から受領したCADデータ（`cad_*`）を協力業者へ公開する。
- **制御ロジック**: 通常の公開フラグ（`is_published_to_sub`）の設定状況に関わらず、発注状態が `requested` または `accepted` の間は強制的に協力業者側へ表示し、辞退または完了後はフラグ通りの制御に戻る。

### ② 協力業者による辞退（拒否）機能
- **仕様**: 協力業者が「承諾待ち」状態の発注タスクに対し、「辞退する」ボタンを押すことで、発注ステータスを `rejected`（辞退済）に更新する。
- **自動チャット通知**: 辞退処理が完了した際、協力業者と管理者のチャットスレッド (`sub_admin`) へ自動で「発注を辞退（拒否）しました。」と通知メッセージが挿入される。
- **CAD非表示**: 辞退された時点で、一時公開されていたCADデータは業者ポータルからアクセス不可（非表示）となる。

---

## 14. 見積シミュレーターの計算仕様 (v1.3.13)

### ① 性能表示壁量計算の価格改修
- **基本料金**: 平屋建（35,000円）と2階建（50,000円）の区分を廃止し、「（性能表示）壁量計算」として**一律 50,000円**とします。
- **人通口箇所数**: 「5箇所箇所以上10箇所未満」の表記の揺れ・誤字を修正し、**「10箇所未満（＋5,000円）」**、10箇所以上（＋10,000円）とします。
- **形状加算（母屋下がり）**: チェックボックス選択式から、**箇所数を数値入力して乗算する方式（1箇所につき＋15,000円）**へ変更します。

---

## 15. 依頼主向け画面での外注情報非表示ルールと「審査・待機」ステータス統一 (v1.4.16)

### ① 協力業者（外注先）関連ステータスの隠蔽
- **仕様**: 依頼主（client）がログインして画面を表示する際、協力業者の存在を示唆するステータス（「作成中 (協力業者ボール)」や「納品検収中 (管理者ボール)」など）を表示してはならない。
- **制御ロジック**: 進行ボールステータス判定（`StatusHelper::getBallStatus`）において、依頼主ロールでアクセスされた場合は、これら外注進捗に関連するステータスをすべて「図書作成中 (管理者ボール)」へ自動で上書き変換して返却する。

### ② 「審査・待機」ステータスのグレー背景統一
- **仕様**: 管理者がスケジュール実績の「申請図書一式UP」に完了日を入力した後の待機フェーズ（確認申請審査中など、アクター: `wait` のステップ）において、管理者画面および依頼主画面の双方で、ステータス表記を「審査・待機」、背景色をグレー（`#64748b`）に完全統一する。
- **制御ロジック**:
  - `StatusHelper.php` のアクター `wait` のステップ、および `submission` / `submitting` ステータス時のフォールバック処理の表示ラベルを「審査・待機」、カラーを `#64748b` に統一する。
  - `$status_labels` および `$status_options` マッピングにおける `submission`, `submitting` の日本語名を「審査・待機」に統一し、基本情報の背景色 `$status_bg` も `#64748b`（グレー）にする。

---

## 17. 協力業者のアクセス制御・招待制限とメール文字化け対策 (v1.5.10)

### ① 協力業者ダッシュボードの開設制限
- **仕様**: 協力業者（`subcontractor`）の新規親アカウント（ポータルダッシュボード）は、登録画面のURL直叩きや外部招待リンクからは一切開設できないように制御する（`invite_role=subcontractor` による直接登録は禁止・遮断する）。
- **開設フロー**: 協力業者の新規（親）ダッシュボードは、必ず管理者がデータベースに対して手動でユーザーを登録することで初めて開設できる仕様とする。

### ② 協力業者子アカウント（スタッフ）の親子ポータル・アクセス共有
- **仕様**: 協力業者代表（親アカウント）から招待されてユーザー登録したスタッフ（子アカウント: `parent_id` を持つユーザー）は、自身の独自ポータルではなく、招待主である代表ユーザー（親アカウント）の案件一覧ポータルに自動的にアクセスする。
- **データ共有**: 支払い管理や進捗一覧は、代表者ベースで一元管理される。

### ③ 子アカウント操作時の権限代替処理
- **仕様**: 子アカウントがログインした状態で案件詳細画面（`project_subcontractor.php`）にアクセスした際、および発注の「承諾」「辞退」「成果物納品（deliveredへの更新）」を行う際は、操作している子アカウントのIDではなく、親アカウント（代表者ID）を基準ID（`$sub_company_id`）として判定・処理を実行する。これによってアクセス権エラーやステータス更新不整合を防止する。

### ④ システム送信メールの文字化け対策
- **仕様**: ログインマジックリンク送信時（`login.php`、`register.php`）および各種通知送信時（`functions.php`）における `mb_send_mail` のマルチバイト言語設定を、一律 **`mb_language("uni")`（UTF-8）** に設定したうえでヘッダーに `Content-Type: text/plain; charset=UTF-8` を付与して送信する。従来の `mb_language("Japanese")`（JIS）との混在によるメーラー側での文字化け（怪しい表示）を完全に防止する。

### ⑤ 依頼主子アカウント（社内スタッフ）の親子ポータル・アクセス共有
- **仕様**: 依頼主代表（親アカウント）から招待されてユーザー登録したスタッフ（子アカウント: `parent_id` を持つユーザー）は、自身の独自ポータルではなく、招待主である代表ユーザー（親アカウント）の案件一覧ポータルに自動的にアクセスする。
- **新規案件作成時の制御**: スタッフ（子アカウント）が新規に見積・計算依頼（`new_request.php`）を作成した際、案件の `client_id` はスタッフ自身のIDではなく、親アカウント（代表者ID）として登録される。これによって親会社全体の案件一覧ポータルに自動的に結合され、一元管理される。

---

## 19. 自動見積もりシミュレーターの仕様変更 (v1.5.22)

### ① 許容応力度計算の床面積加算ロジック（累進スラブ計算）
- **仕様**: これまで「150㎡超が一律600円/㎡加算」だった許容応力度計算の構造床面積割増について、床面積（構造床面積）の大きさに応じた以下の累進加算（段階スラブ計算）に変更する。
  - **150㎡超〜300㎡未満の部分（最大150㎡分）**: 600円/㎡
  - **300㎡以上〜500㎡未満の部分（最大200㎡分）**: 500円/㎡
  - **500㎡以上の部分**: 400円/㎡
- **表示仕様**: 見積計算および画面上の明細行（PDF出力含む）は、ユーザーが計算式を明確に追えるよう、該当する段階ごとに明細を分けて出力する。
  - 例：構造床面積が600㎡の場合、以下の3行に分けて割増明細が追加される。
    - 構造床面積割増 (150㎡〜300㎡未満): 150㎡ × 600円 = 90,000円
    - 構造床面積割増 (300㎡〜500㎡未満): 200㎡ × 500円 = 100,000円
    - 構造床面積割増 (500㎡以上): 100㎡ × 400円 = 40,000円

### ② 手動追加明細における金額の負の値（値引き）の許可
- **仕様**: 管理者が見積シミュレーター上で手動で明細（値引きや調整額など）を追加する際、金額欄に **「-（マイナス）」** の数値を入力できるようにする。
- **入力バリデーション**:
  - 金額入力フィールドでの正規表現置換処理において、先頭に1文字だけマイナス記号の入力を許可し、余分な記号や不正な入力値を除去する。
  - `pattern` 属性を `^-?[0-9]*` にし、マイナスから始まる整数値を受け入れ可能にする。

---

## 21. 新規見積完了時の画面リダイレクト化、および管理者ダッシュボードでの協力業者納期・タスク表示 (v1.5.24)

### ① 新規見積・計算依頼完了時のモーダル廃止およびダッシュボード直接遷移
- **仕様**: 依頼主が新規で見積・計算依頼を送信した完了直後に表示されていた「確認メール送信完了モーダル（`emailConfirmModal`）」を完全に廃失し、送信完了後は自動的にダッシュボード（`index.php`）へリダイレクトする。
- **お知らせ表示**: ダッシュボードの画面上部に緑色のアラート領域で「見積依頼を受け付けました。〜宛に確認メールを送信しました。※迷惑メールフォルダもご確認ください（送信元: system@thanks.work）」とメッセージを自動表示する。これによってモーダルを手動で閉じる手間を省き、かつ以前の完了セッションが残って新規見積画面を開いた際に関係ないモーダルが誤爆表示されるバグを解消する。

### ② 管理者ダッシュボードでの協力業者進行中タスク・納期の表示拡張
- **仕様**: 管理者の案件ダッシュボードにおいて、ステータスが「協力業者ボール（`ball_owner = subcontractor`）」になっている案件については、スケジュール予定日表示エリアを以下の情報に置き換えて詳細に表示する。
  - **進行中の協力業者タスク**: 現在依頼中（`requested`）または進行中（`accepted`）、あるいは修正依頼中（`cb_requested`）である `subcontractor_orders` からタスクタイトルを取得し、「意匠図作図」または「構造図作図」の種別と合わせて表示する。
  - **納品予定日（納期）**: 該当する協力業者タスクの `due_date` を取得し、赤字等で分かりやすくダッシュボードに表示する。

---

## 22. 中間金報告、通知トグル設定、協力業者未承諾表示、および3週間後リマインダー (v1.5.25)

### ① 中間金（50％）報告の自動登録および経理への通知
- **仕様**: 依頼主ポータル（`dashboard_client.php`）の基本情報ボックスに「💵 中間金（50％）を入金しました」という報告ボタンを設置。ボタンが押されると、対応する各計算種別のスケジュール実績における「中間金（50％）のご入金」ステップに本日日付を自動登録する。同時に、経理担当者（`role = 'accountant'`）の中でメール通知がONの全員に対して、入金報告があった旨の通知メールを自動送信する。
- **審査完了（残金精算）との連携**: 「審査完了にする（審査合格）」ボタンの表示を「残金お振込み ＆ 審査完了にする（審査合格）」に変更。ボタン押下による完了登録時に、経理担当者へ残金精算開始の通知メールを自動送信する。

### ② 依頼主ポータルのスタッフオンラインアイコンおよび通知トグルスイッチ
- **仕様**: 依頼主ポータルのヘッダー近くに、同一親IDに属するログイン可能な全スタッフ（子ユーザー含む）の丸アバターアイコンを横並びで表示する。
- **オンライン判定**: ユーザーの `last_active_at` カラムの最終更新日時が過去5分以内の場合は緑色のドット（オンライン）、それ以外はグレーのドットで表示する。
- **個別通知設定 (Ajax)**: ログインしている自分自身のアイコンをクリックすると、メール通知の受信可否を切り替えるトグルスイッチが表示される。変更は `api_update_notification.php` を経由してDBの `email_notification_enabled` (1/0) に即時保存される。すべての `sendSystemEmail` 処理において、送信前にこの設定を参照してオプトイン制御を行う。

### ③ 協力業者「承諾待ち（未承諾）」の管理者ダッシュボードでの明示
- **仕様**: 管理者ダッシュボードの協力業者ボール案件一覧において、希望納期を発注したものの協力業者がまだ承諾していない（`status = 'requested'` / `cb_requested`）状態の案件カードについて、納期エリアの隣に赤字で「⚠️ 未承諾 (承諾待ち)」と明示的に表示させる。

### ④ 「補正対応」完了3週間後の審査状況確認リマインダーの自動化 (Cron)
- **仕様**: 「補正対応」マイルストーンに完了日（実施日）が入力されてからちょうど21日（3週間）が経過しても、案件のステータスが「完了（`completed`）」になっていない場合、毎日1回定期実行されるバッチスクリプト `cron_reminder_inspection.php` が起動する。
- **リマインダー送信**: 対象案件の依頼主（通知ONの全スタッフ）宛てに、確認申請の審査進捗状況の確認と、残金精算の事前準備を促す案内メールを自動で一斉送信し、送信済みフラグ `inspection_reminder_sent = 1` に更新して重複送信を防ぐ。

---

## 23. 依頼主ダッシュボードでのPDOプレースホルダー重複バグ修正 (v1.5.26)
- **仕様**: 依頼主ポータルのスタッフ一覧を取得するクエリにおいて、同一のプレースホルダー名（`:cid`）を複数回使用してバインドすると、PDOの `PDO::ATTR_EMULATE_PREPARES => false` 設定下でバインドエラーが発生するバグを解消。`:cid1` と `:cid2` のようにプレースホルダー名を分離して個別にパラメータバインドするように修正した。

---

## 24. 財務入金日とスケジュール実績日の双方向同期とダッシュボード分離 (v1.5.27)
- **仕様1 (双方向同期)**: 経理が「着手金(50%)の入金日」および「残金の入金日」を保存した際、スケジュール実績の「中間金（50％）のご入金」および「残金のご精算」ステップに自動同期。逆に、依頼主ポータル等でスケジュール実績が入力された際にも経理入金日へ同期。既存データについてもマイグレーションスクリプト `migrate_sync_existing_finance_schedule.php` で一括同期を適用。
- **仕様2 (新規登録導線)**: ログイン画面 `login.php` のフォーム下部に新規登録画面 `register.php` への直接リンクを新設。
- **仕様3 (管理者ダッシュボード分離)**: メインダッシュボード `index.php` から見積中（`quote_req`）の案件を非表示にし、新設する見積中案件専用ダッシュボード `admin_estimates.php` に分離。依頼主が設計依頼した段階で自動的にメインダッシュボードに移行。
- **仕様4 (自動・手動アーカイブ)**: 見積発行から30日経過した見積中案件は `cron_archive_estimates.php` によって自動的に `is_archived = 1` （アーカイブ）へ移行。依頼主側でも見積中カードに「非表示」ボタンを設置し、`is_client_archived = 1` に更新して手動アーカイブ可能に。アーカイブされた案件は `admin_estimates.php` から復旧（以前の見積データをクリアして再見積開始）可能。

---

## 25. このドキュメントの所在

- **AIエージェント用ドキュメント**: `C:\Users\user\.gemini\antigravity-ide\brain\512b86dc-9f28-471d-8567-535aee35248c\FIXED_LOGIC.md`
- **システム仕様書（GEMINI.md）**: `e:\Dropbox\■設計ｻﾎﾟｰﾄ\■note\antigravity\system\gemini.md`
- **最終バージョン**: v1.5.27（2026-07-03）




